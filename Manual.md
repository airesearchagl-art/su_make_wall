# MakeWall 操作マニュアル

MakeWall は、クリックとマウス移動だけで、厚みと高さを持った「壁」を連続して素早く作成できる SketchUp 専用ツールです。

---

## 1. ツールの起動方法

以下のいずれかの方法で MakeWall ツールを起動できます。

| 方法 | 操作 |
|---|---|
| ツールバー | 画面上の MakeWall アイコンをクリック |
| メニュー | `拡張機能 (Extensions)` > `MakeWall` |
| 右クリック | 何も選択していない状態で右クリック > `MakeWall` |

起動すると、画面の端に **パラメータ設定パネル** が表示されます。

---

## 2. 基本的な壁の描き方

1. **始点を決める** — 3D 空間上の任意の場所をクリックします。
2. **マウスを動かす** — 壁のプレビュー（ワイヤーフレーム）がマウスに追従します。マイター処理で変形する端部には赤い「×」マークが表示されます。
3. **終点を決める** — 次の点をクリックすると、指定した高さ・厚みの壁（グループ）が立ち上がります。
4. **連続して描く** — そのままクリックを続けると、コーナーが自動的に包絡された壁が連続して作成されます。
5. **ループを閉じる** — 一周して「最初の始点」をクリックすると、最初の壁と最後の壁が自動接合されて図形が完成します。

---

## 3. 描画のキャンセルと終了

| 操作 | 動作 |
|---|---|
| `ESC` または右クリック（描画中） | 連続描画を区切り、新しい始点から描き直せる状態に戻る |
| `ESC`（待機中） | MakeWall を終了して選択ツールに戻る |
| 別のツールに切り替え | MakeWall を終了する |

---

## 4. 正確な寸法・方向で作図する

### 4-1. 矢印キーで方向を固定（軸ロック）

始点をクリックした後、矢印キーを押すと描画方向がロックされます。

| キー | ロック軸 | 色 |
|---|---|---|
| `→` | X 軸（赤軸） | 赤 |
| `←` | Y 軸（緑軸） | 緑 |
| `↑` | Z 軸（青軸） | 青 |
| `↓` | ロック解除 | — |

### 4-2. キーボードで長さを指定（VCB 数値入力）

マウスで方向を示した状態（または軸ロックした状態）で、数値を入力して `Enter` を押します。

| 入力例 | 結果 |
|---|---|
| `3000` または `3000mm` | 3,000 mm（3 m）の壁を生成 |
| `3.0m` | メートル単位での指定も可能 |

---

## 5. パラメータ設定パネル

画面端のパネルでは、作図中であってもリアルタイムに以下の設定を変更できます。

| 項目 | 説明 | スピン刻み |
|---|---|---|
| **高さ** | 壁の高さ（mm） | 50 mm |
| **厚み** | 壁の厚み（mm） | 10 mm |
| **基準線** | 描画線に対する壁の寄り方：`中心 (center)` / `左端 (left)` / `右端 (right)` | — |

> **基準線について**
> `左端` は描画方向に対して左側、`右端` は右側に壁のボリュームが生成されます。
> 変更はパネルのドロップダウンを切り替えた瞬間にプレビューへ反映されます。

---

## 6. Undo（元に戻す）

1 本の壁（またはループ閉合操作）が 1 ステップの Undo に対応しています。`Ctrl + Z`（macOS: `Cmd + Z`）で直前の壁のみを取り消せます。

---

## 7. ショートカット一覧

| キー | 動作 |
|---|---|
| 左クリック | 始点 / 終点の確定 |
| 右クリック | 連続描画のリセット |
| `ESC` | 描画中: リセット　／　待機中: ツール終了 |
| `→` | 赤軸（X）ロック |
| `←` | 緑軸（Y）ロック |
| `↑` | 青軸（Z）ロック |
| `↓` | 軸ロック解除 |
| 数値 + `Enter` | 長さを指定して壁を確定（VCB 入力） |
| `Ctrl + Z` | 直前の壁を Undo |
